{
  "//": "OpenCues compatibility manifest for Claude Code. Read by `opencues update claude-code [--check|--to <version>]`. Keep in sync with manual testing — every time someone validates a new claude-code version, add it to `tested` (and bump `current-pin` if you want it to become the default).",
  "host-kind": "npm",
  "host-package": "@anthropic-ai/claude-code",
  "current-pin": "2.1.206",
  "tweakcc-pin": "1545ff83379b410199029c623a42df3f3f057cdc",
  "//tweakcc-pin": "Commit of Piebald-AI/tweakcc that setup.sh checks out after clone (validated against current-pin — 1545ff8 = 'Prompts for 2.1.206', v4.3.0+20). tweakcc ships per-CC-version prompt regexes AND is the patch engine, so an unpinned clone drifts independently of the CC pin: a future tweakcc main could move the applySystemPrompts callsite (breaking setup.sh § 4e's anchor) or regress the .bun repack. Bump this TOGETHER with current-pin after re-running the UPGRADING.md dance — tweakcc main needs a 'Prompts for <new-version>' commit before a CC bump can be validated.",
  "pin-location": {
    "kind": "npm-package-json",
    "path-from-fork": "package.json",
    "field": "dependencies.@anthropic-ai/claude-code",
    "fork-default": "~/claude-code-cues"
  },
  "tested": ["2.1.110", "2.1.150", "2.1.158", "2.1.170", "2.1.206"],
  "compat-range": "2.1.x",
  "//notes": {
    "binary-cutover": "2.1.113 (not 2.1.119 — that was an earlier guess). From 2.1.113 onward, @anthropic-ai/claude-code ships a thin Node wrapper + a bun-compile native binary in @anthropic-ai/claude-code-<platform>. Tweakcc 4.0.13+ extracts cli.js from the binary's .bun ELF section, patches the text, and repacks (handles ELF/MachO/PE). Same tweakcc anchor regexes work on both formats; the cli.js text inside the .bun section runs unchanged at startup (bun honours text over its own precompiled bytecode).",
    "native-binary-fork": "For 2.1.113+ installs, the patched binary lives at <fork>/node_modules/@anthropic-ai/claude-code/bin/claude.exe (a hardlink to claude-code-linux-x64/claude or similar). @opencues/{core,runtime} go in <fork>/node_modules/@opencues/. The injected bootstrap uses __oc_req() — tries bare-spec first, falls back to walking up from process.execPath looking for node_modules/<spec> — so the same patch source works against both cli.js and native-binary installs."
  }
}
