{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://opencues.org/schemas/blanks-master.schema.json",
  "title": "OpenCues BLANKS.md master frontmatter",
  "description": "JSON Schema for the YAML frontmatter of a BLANKS.md master file. Spec: opencues/0.1-alpha. See /spec/core.md § Master files.",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Identifies the project (or user library) as a whole."
    },
    "description": {
      "type": "string",
      "description": "Human-readable summary."
    },
    "spec": {
      "type": "string",
      "pattern": "^opencues/\\d+\\.\\d+(-[a-z]+)?$",
      "default": "opencues/0.1-alpha"
    },
    "ignore": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Tokens the runtime never auto-fills, regardless of any blank match."
    },
    "disable": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Blank source ids excluded at this layer."
    }
  },
  "additionalProperties": true
}
