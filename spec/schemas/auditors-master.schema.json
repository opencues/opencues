{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://opencues.org/schemas/auditors-master.schema.json",
  "title": "OpenCues AUDITORS.md master frontmatter",
  "description": "JSON Schema for the YAML frontmatter of an AUDITORS.md master file. Spec: opencues/0.1-alpha. See /spec/core.md § Master files and /spec/auditor-spec.md § Master file.",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Identifies the project (or user library) as a whole."
    },
    "description": {
      "type": "string",
      "description": "Human-readable summary."
    },
    "spec": {
      "type": "string",
      "pattern": "^opencues/\\d+\\.\\d+(-[a-z]+)?$",
      "default": "opencues/0.1-alpha"
    },
    "disable": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Auditor source ids excluded at this layer. Subtracts from the user→project ADD-by-default composition without modifying the user library."
    }
  },
  "additionalProperties": true
}
