---
name: example-project
description: Blank-surface master for a project
spec: opencues/0.1-alpha
ignore: [_placeholder]
disable: [stocks]
---

Optional body. `ignore:` here scopes to blanks — `_placeholder` is treated as a literal `_`-prefixed token that never auto-fills. `disable:` subtracts the named blank from this layer.
