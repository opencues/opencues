---
name: example-project
description: Auditor-surface master for a project
spec: opencues/0.1-alpha
disable: [clarity]
---

This project disables the `clarity` auditor (codebase has intentionally informal prose) while keeping every other user-level auditor active. cd out of the project, `clarity` fires again.
