---
name: tips-gemini-cli
# Tip pack scoped to the Gemini CLI integration. See `defaults/cues/tips/CUE.md`
# for the schema reference and `defaults/cues/tips-opencode/CUE.md` for a
# minimal stub. The folder-loader's host-compat filter skips this folder
# on every other host so the trigger words never collide.
on-host: [gemini-cli]
---

```json
[]
```
