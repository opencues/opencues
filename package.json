{
  "name": "opencues",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r --parallel --if-present test",
    "typecheck": "pnpm -r --parallel --if-present typecheck",
    "clean": "rm -rf packages/*/dist integrations/chrome-extension/dist"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
