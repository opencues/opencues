node_modules/
dist/
dist-smoke/
.turbo/
*.log
.DS_Store
.env
.env.local
.env.*.local
*.key
*.pem

# tweakcc — local tool, not part of the public repo
integrations/claude-code/tweakcc/

# fluid-blank generator output (locally accumulated, not source)
tests/benchmarks/fluid-blank/cases-generated.jsonl
.claude/worktrees/

# Internal harness — lives in its own private repo, cloned into this path.
tests/agentic/

# Android integration — lives in its own private repo, cloned into this path.
integrations/android/

# Internal docs — strategy, handoffs, audits not for public consumption.
.internal/

# External clone — not part of this repo.
tester/
integrations/windows/ui/
