{INDEXED_WORDS}

You are helping a user learn Claude Code CLI terms. ONLY provide alternatives for recognized Claude Code terms below. Skip ALL other words.

RECOGNIZED TERMS AND THEIR ALTERNATIVES:

Slash commands:
- /help → /doctor, /status
- /clear → /compact, /rewind
- /compact → /clear, /context
- /rewind → /clear, Esc x2
- /plan → Shift+Tab x2, /model
- /model → haiku, sonnet, opus
- /config → /permissions, /memory
- /resume → /continue, --resume
- /mcp → /plugin, .mcp.json
- /doctor → /help, /status
- /status → /doctor, /help
- /usage → /cost, /stats
- /cost → /usage, /stats
- /teleport → /tp, claude.ai

Magic words (trigger special behavior):
- ultrathink → "deep thinking", "max reasoning"
- sub-agents → swarm, parallel, "multiple agents"
- swarm → sub-agents, parallel, "spawn workers"
- parallel → sub-agents, swarm, concurrently

Keyboard shortcuts:
- Shift+Tab → /plan (x2), auto-approve (x1)
- Ctrl+B → background, /tasks
- Esc → cancel, /rewind (x2)

Model names:
- haiku → sonnet, opus, "fast model"
- sonnet → haiku, opus, "default"
- opus → haiku, sonnet, "max capability"

RULES:
1. ONLY output alternatives for terms listed above
2. Skip common words (use, for, this, the, to, etc.)
3. Skip words not in the recognized terms list

Format: INDEX:alt1,alt2,alt3|INDEX:alt1,alt2
Example: 4:swarm,parallel|7:sonnet,haiku

Output ONLY index:alternatives format. No explanation.
If no Claude Code terms found, output: NONE
