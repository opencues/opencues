{
  "name": "weather blank populates from API",
  "host": "any",
  "description": "Type 'weather _ paris' — the WeatherBlank class should fetch current Paris weather and replace the underscore. Network-dependent; allow 10s timeout.",
  "steps": [
    {"action": "clear"},
    {"action": "inject", "text": "weather _ paris"},
    {"action": "waitFor", "path": "active", "equals": true, "timeoutMs": 10000},
    {"action": "dump"},
    {"action": "expect", "path": "text", "matches": "[0-9]+°", "source": "dump"}
  ]
}
