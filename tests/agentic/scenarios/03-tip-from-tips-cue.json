{
  "name": "tip-mode cue resolves locally — instant, no LLM call",
  "host": "any",
  "description": "Words present in defaults/cues/tips/CUE.md (e.g. 'ultrathink') get a tip + alts via local lookup. No LLM round-trip — should resolve well under 1s.",
  "steps": [
    {"action": "clear"},
    {"action": "inject", "text": "we should ultrathink this approach"},
    {"action": "key", "key": "right", "modifiers": ["ctrl", "alt"]},
    {"action": "waitForEvent", "type": "highlight.activated", "path": "word", "equals": "ultrathink", "timeoutMs": 1500},
    {"action": "sleep", "ms": 200},
    {"action": "expect", "path": "cueTip", "matches": "ultrathink"}
  ]
}
